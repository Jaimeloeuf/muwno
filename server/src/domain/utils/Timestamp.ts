/**
 * Type alias so that ISO DateTime strings have an explicit label,
 * rather than being all typed as simply `string`.
 *
 * This is also the type of DateTime stored in PostgreSQL DB.
 */
export type ISODateTimeString = string;
