// Import to run the side effect of 'removeSW'
import "../sw/removeSW";

// Import to run the side effect of 'reloadPageAfter24Hours'
import "./reloadPageAfter24Hours";

// Import to run the side effect of setting base Urls before it is used elsewhere
import "./API";

// Import to run the side effect of setting up firebase app.
import "./firebase";
